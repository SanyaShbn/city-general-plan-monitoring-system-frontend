import React from 'react';
import { Typography, Box } from '@mui/material';

export default function ExportPage() {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>Экспорт данных</Typography>
      <Typography>Выбор формата, диапазона, генерация файла.</Typography>
    </Box>
  );
}
